{
  "name": "nodetreeexporter",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "tree-export": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "tree-export": "./src/index.js"
  },
  "keywords": [
    "node",
    "directory",
    "tree",
    "export"
  ],
  "author": "EtnasSoft",
  "license": "ISC",
  "description": "A Node.js script to export the directory tree structure to a text file.",
  "dependencies": {
    "fs-extra": "^11.1.1",
    "minimatch": "^10.0.3"
  }
}
